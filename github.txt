# 1. НАЗВАНИЕ РАБОЧЕГО ПРОЦЕССА
name: CI_CD  # Произвольное имя workflow

# 2. ТРИГГЕРЫ ЗАПУСКА
on:
  push:  # Запуск при push в репозиторий
    branches: [ "main" ]  # Только для ветки main
  pull_request:  # Запуск при создании/обновлении PR
    branches: [ "main" ]  # Только для PR в main

# 3. ПЕРЕМЕННЫЕ СРЕДЫ (опционально)
env:

# 4. ДЖОБЫ (основные задачи)
jobs:
  test:  # Уникальное имя джоба
    # Конфигурация окружения
    runs-on: ubuntu-latest  # Например, ОС для запуска

    # ШАГИ ВЫПОЛНЕНИЯ
    steps:
      - name: Checkout repository  # Название шага
        uses: actions/checkout@v3  # Используемое действие (action)
        with:  # Параметры действия (опционально)
        run: |  # Выполнение shell-команд
